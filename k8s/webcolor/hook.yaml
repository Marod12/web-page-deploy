apiVersion: batch/v1
kind: Job
metadata:
  generateName: pre-sync-
  annotations:
    argocd.argoproj.io/hook: PreSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
        - name: chamada
          image: curlimages/curl:latest
          command:
            - /bin/sh
            - -c
            - | 
              curl -X POST -H "Content-Type: application/json" \
              -d '{"message": "Entrou em Pre Sync !!"}' \
              https://purple-griffin-14.webhook.cool
      restartPolicy: Never

---

apiVersion: batch/v1
kind: Job
metadata:
  generateName: sync-
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
        - name: chamada
          image: curlimages/curl:latest
          command:
            - /bin/sh
            - -c
            - | 
              curl -X POST -H "Content-Type: application/json" \
              -d '{"message": "Entrou em Sync !!"}' \
              https://purple-griffin-14.webhook.cool
      restartPolicy: Never

---

apiVersion: batch/v1
kind: Job
metadata:
  generateName: post-sync-
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
        - name: chamada
          image: curlimages/curl:latest
          command:
            - /bin/sh
            - -c
            - | 
              curl -X POST -H "Content-Type: application/json" \
              -d '{"message": "Entrou em Post Sync !!"}' \
              https://purple-griffin-14.webhook.cool
      restartPolicy: Never
